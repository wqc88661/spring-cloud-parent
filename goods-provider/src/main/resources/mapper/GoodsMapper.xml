<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.goodsprovider.dao.GoodsDao">
    <select id="getAllGoods" resultType="com.itheima.Goods">
       select * from goods
    </select>
    <select id="getLikeGoods" resultType="com.itheima.Goods" parameterType="java.lang.String">
        SELECT * FROM goods WHERE gname LIKE "%${gname}%"
    </select>
     <select id="getIdGoods" resultType="com.itheima.Goods" parameterType="int">
         select * from goods where gid=#{gid}
     </select>
</mapper>